# 1181 단어 정렬

<br>

<table width="100%">
  <tr>
    <td width="20%"><b>Solved Date</b></td>
    <td width="30%">2026-04-17</td>
    <td width="20%"><b>Platform</b></td>
    <td width="30%"><img src="https://img.shields.io/badge/BOJ-0076C0?style=flat-square&logo=baekjoon&logoColor=white"> <img src="https://img.shields.io/badge/Silver 5-435F7A?style=flat-square"></td>
  </tr>
  <tr>
    <td><b>Category</b></td>
    <td colspan="3">
      <img src="https://img.shields.io/badge/String-DB2777?style=flat-square">
      <img src="https://img.shields.io/badge/Sorting-4A5568?style=flat-square">
    </td>
  </tr>
</table>

<br>

## [Problem Link](https://www.acmicpc.net/problem/1181)

<br>

## Strategy

1. 접근 방식
   - N개의 단어를 입력받아 다음 두 가지 조건으로 정렬해야 함: 1) 길이가 짧은 것부터 2) 길이가 같으면 사전 순
   - Set으로 중복 먼저 제거 -> 정렬
2. 시행착오
   - Word 객체 만들어서 `Set`에 넣음 -> 중복 제거 안 됨
   - 커스텀 객체를 만들어 `Set`에 넣을 경우 `equals()`와 `hashCode()` 오버라이딩을 누락하면 중복 제거가 되지 않기 때문.
   - 객체로 만들어서 넣으려면 `equals()`와 `hashCode()`해줘야 함!
   - String` 자체를 `HashSet`에 넣어 중복을 제거하고, 이후에 `ArrayList`로 변환하여 정렬하는 것으로 변경

<br>

## Solution

```java
import java.io.*;
import java.util.*;

public class BOJ1181 {

    public static void main(String[] args) throws IOException {

        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StringBuilder sb = new StringBuilder();
        int N = Integer.parseInt(br.readLine());

        // 중복 제거
        HashSet<String> set = new HashSet<>();
        for (int i = 0; i < N; i++) {
            set.add(br.readLine());
        }

        // Set -> List
        List<String> list = new ArrayList<>(set);

        // 정렬 
        list.sort((s1, s2) -> {
            if (s1.length() == s2.length()) {
                return s1.compareTo(s2); // 2. 사전순
            }
            return s1.length() - s2.length(); // 1. 길이순
        });

        // 출력
        for (String word : list) {
            sb.append(word).append("\n");
        }
        System.out.println(sb);
    }
}
```

<br>

## Retrospective

- **To Improve**: 람다함수 정렬 방식 암기하기
